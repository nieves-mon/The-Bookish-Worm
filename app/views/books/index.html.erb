<h1>All Books</h1>

<div>
    <% @books.each do |book| %>
        <div>
            <p><%= book.title %></p>
            <span><%= link_to "View",  book_path(book.id) %></span>
        </div>
    <% end %>
</div>
